% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/handlers.R
\name{emit_shower}
\alias{emit_shower}
\title{Emit Emoji Shower from Server}
\usage{
emit_shower(session)
}
\arguments{
\item{session}{Shiny session object. This is automatically passed to your
server function as an argument. It provides the connection to communicate
with the client-side JavaScript code.}
}
\value{
NULL (invisibly). The function operates for its side effect of sending
a message to the client to trigger the animation. The return value is
intentionally invisible to avoid printing NULL in interactive contexts.
}
\description{
A convenient wrapper function to trigger an emoji shower animation from your
Shiny server code. This is the recommended way to manually trigger showers
instead of calling \code{session$sendCustomMessage()} directly.
}
\details{
\subsection{Requirements}{

This function must be used within a Shiny server function. Additionally,
\code{\link[=setup_emoji_handler]{setup_emoji_handler()}} must be included in your app's UI, and \code{\link[=emoji_shower_ui]{emoji_shower_ui()}}
must have \code{trigger = NULL} set.
}

\subsection{Usage}{

Use within any reactive context (observeEvent, reactive, etc.):

\if{html}{\out{<div class="sourceCode r">}}\preformatted{observeEvent(input$my_button, \{
  emit_shower(session)
\})
}\if{html}{\out{</div>}}

Multiple calls to \code{emit_shower()} will queue animations sequentially.
The shower uses the configuration (duration, particles, speed, etc.) set in
the original \code{\link[=emoji_shower_ui]{emoji_shower_ui()}} call.
}

\subsection{Error Handling}{

The function validates that \code{session} is a proper Shiny session object
and throws an informative error if an invalid session is provided.
}
}
\examples{
\dontrun{
server <- function(input, output, session) {
  observeEvent(input$celebrate_btn, {
    emit_shower(session)
  })
}
}

}
